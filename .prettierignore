node_modules/
.next/
out/
build/
dist/
coverage/
package-lock.json
*.tgz

# Generated by `npm run codegen` — written verbatim from the API's artifacts.
# The ts-zod emitter targets Prettier's DEFAULTS (80 cols); this repo prints at
# 100, so formatting these would rewrite bytes, break every stamp, and make
# `npm run codegen:check` report the whole tree as hand-edited.
src/generated/

# Scratch written by local developer tooling, ignored by a nested `.gitignore`
# of its own — which Prettier does not read (it consults the ROOT `.gitignore`
# and this file), so without the entry `make format-check` fails on a file that
# is not the repo's at all.
.gstack/
