// >>> pipelex-codegen-stamp >>>
// crate_fingerprint: 9a4af3ab3ff33ee8c8ad83c23cb4702346e1adfae8c64660a6dd64eae80c663a
// engine_version: 0.52.0
// projection: types / ts-zod
// options: {}
// content_hash: 1eb559b873934b0ce1f79d76c1566f1f32c184cd7c2a92cfce8b541f8b37c129
// <<< pipelex-codegen-stamp <<<
// ---------------------------------------------------------------------------
// AUTOGENERATED by Pipelex codegen — DO NOT EDIT.
//
// Typed parse/serialize helpers over the pure `types.ts` schemas. Field keys are
// wire-native (snake_case), so parse/serialize validate the wire payload directly.
//
// projection: types / ts-zod (binder)
// ---------------------------------------------------------------------------
import {
  ExtractionBriefSchema,
  type ExtractionBrief,
  ExtractionFocusSchema,
  type ExtractionFocus,
  TextSchema,
  type Text,
} from "./types";

/** Parse a wire payload into a validated `ExtractionBrief`. */
export function parseExtractionBrief(wire: unknown): ExtractionBrief {
  return ExtractionBriefSchema.parse(wire);
}

/** Validate a `ExtractionBrief` for the wire (keys are already wire-native). */
export function serializeExtractionBrief(
  value: ExtractionBrief,
): ExtractionBrief {
  return ExtractionBriefSchema.parse(value);
}

/** Parse a wire payload into a validated `ExtractionFocus`. */
export function parseExtractionFocus(wire: unknown): ExtractionFocus {
  return ExtractionFocusSchema.parse(wire);
}

/** Validate a `ExtractionFocus` for the wire (keys are already wire-native). */
export function serializeExtractionFocus(
  value: ExtractionFocus,
): ExtractionFocus {
  return ExtractionFocusSchema.parse(value);
}

/** Parse a wire payload into a validated `Text`. */
export function parseText(wire: unknown): Text {
  return TextSchema.parse(wire);
}

/** Validate a `Text` for the wire (keys are already wire-native). */
export function serializeText(value: Text): Text {
  return TextSchema.parse(value);
}
